<div data-card-view data-card="{{ data|json_encode|e('html_attr') }}"><canvas width="1200" height="756" aria-label="MILSIM identification card for {{ data.name }}">{{ data.name }} — Member ID {{ data.memberId }} — NOT A GOVERNMENT ID</canvas></div>
